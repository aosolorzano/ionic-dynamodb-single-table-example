import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-amplify-graphql-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
